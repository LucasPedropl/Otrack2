rules_version = '2';
service lib.firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permite leitura e escrita se o usuário estiver autenticado
      allow read, write: if request.auth != null;
    }
  }
}